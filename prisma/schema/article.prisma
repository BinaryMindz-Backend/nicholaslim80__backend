model article {
  id        Int      @id @default(autoincrement())
  title     String
  faq_for   UserRole @default(USER)
  content   String
  published Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
