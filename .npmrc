# pnpm supply-chain policy
# Disables the minimumReleaseAge check that blocks newly published packages.
# This is needed because transitive optional deps (e.g. fast-xml-parser -> path-expression-matcher)
# can be resolved to versions published within the cutoff window, breaking --frozen-lockfile installs.
minimum-release-age=0
